# secrets (never commit)
.env
.env.local
.env.*.local

node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# IDE (개인 로컬 설정 — 저장소에 올리지 않음)
.idea/** 
.vscode/**

# Java / Gradle build outputs
.gradle/
build/
target/

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

# TDD Guard runtime compatibility data generated by Codex hooks
.codex/tdd-guard/compat/
